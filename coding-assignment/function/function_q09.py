# Function to calculate the average of numbers in a list
def average_of_list(lst):
    return sum(lst) / len(lst)

# Example usage
lst = [10, 20, 30, 40, 50]
print(f"The average of the list is {average_of_list(lst)}.")
